import Layout from '../layouts/LessonLayout.astro';

export const schedule_all = [
	{ date: "2026-10-20", speaker: "Jaeyeon Kim", website: "https://jaeyeonkim01.github.io/", title: "From Selective Underfitting to One-Step Generative Modeling", abstract: "Diffusion models are trained to approximate the score of a finite empirical distribution, yet accurately matching this score everywhere would lead them to reproduce their training data rather than generate novel samples. In the first part of this talk, I will introduce selective underfitting, a framework for understanding this apparent paradox better. We distinguish between a supervision region, where noisy training samples concentrate and the empirical score is directly learned, and an extrapolation region, which receives little direct supervision but is where the model is queried during inference. We find that better generative models fit the empirical score more accurately within the supervision region while selectively deviating from it outside, suggesting that generative performance depends not only on how much a model underfits, but also on where it does so. In the latter half of the talk, I will present a simple approach to one-step generation using the insight gained from selective underfitting. Our preliminary analysis shows that off-manifold error decreases throughout training and is further reduced by distribution matching, leading to a one-step generative model. Together, these results suggest a geometric view of generative modeling." },
	{ date: "2026-10-13", speaker: "TBD", website: "", title: "", abstract: "TBD" },
	{ date: "2026-10-06", speaker: "TBD", website: "", title: "", abstract: "TBD" },
	{ date: "2026-09-22", speaker: "", website: "", title: "ICLR Break", abstract: "" },
	{ date: "2026-09-15", speaker: "", website: "", title: "ICLR Break", abstract: "" },
	{ date: "2026-09-29", speaker: "Samuel Boïté", website: "https://samuelbx.github.io/", title: "Expected Batch Optimal Transport Plans and Consequences for Flow Matching", abstract: "TBD" },
	{ date: "2026-09-08", speaker: "Renjie Liao", website: "https://lrjconan.github.io/", title: "Stable Velocity: A Variance Perspective on Flow Matching", abstract: "While flow matching is elegant, its reliance on single-sample conditional velocities leads to highvariance training targets that destabilize optimization and slow convergence. By explicitly characterizing this variance, we identify 1) a highvariance regime near the prior, where optimization is challenging, and 2) a low-variance regime near the data distribution, where conditional and marginal velocities nearly coincide. Leveraging this insight, we propose Stable Velocity, a unified framework that improves both training and sampling. For training, we introduce Stable Velocity Matching (StableVM), an unbiased variancereduction objective, along with Variance-Aware Representation Alignment (VA-REPA), which adaptively strengthen auxiliary supervision in the low-variance regime. For inference, we show that dynamics in the low-variance regime admit closed-form simplifications, enabling Stable Velocity Sampling (StableVS), a finetuning-free acceleration. Extensive experiments on ImageNet 256 * 256 and large pretrained text-to-image and text-to-video models, including SD3.5, Flux, Qwen-Image, and Wan2.2, demonstrate consistent improvements in training efficiency and more than 2× faster sampling within the low-variance regime without degrading sample quality. Our code is available at https://github.com/linYDTHU/StableVelocity." },
	{ date: "2026-09-08", speaker: "Nidham Gazagnadou", website: "https://ngazagna.github.io/", title: "On Adaptivity in Zeroth-Order Optimization", abstract: "We investigate the effectiveness of adaptive zeroth-order (ZO) optimization for memory-constrained fine-tuning of large language models (LLMs). Contrary to prior claims, we show that adaptive ZO methods such as ZO-Adam offer no convergence advantage over well-tuned ZO-SGD, while incurring significant memory overhead. Our analysis reveals that in high dimensions, ZO gradients lack coordinate-wise heterogeneity, rendering adaptive mechanisms memory inefficient. Leveraging this insight, we propose MEAZO, a memory-efficient adaptive ZO optimizer that tracks only a single scalar for global step size adaptation. We support our method with theoretical convergence guarantees under standard assumptions. Experiments across multiple LLM families and tasks demonstrate that MEAZO matches ZO-Adam's performance with the memory footprint of ZO-SGD. Additional experiments on synthetic quadratic problems and LLM fine-tuning further demonstrate MEAZO's enhanced robustness to step size choices, particularly in grouped or block-structured optimization settings." },
	{ date: "2026-09-01", speaker: "Danilo Vucetic", website: "https://mila.quebec/en/directory/danilo-vucetic", title: "Recommender Regret for Sampling in Bandits and RL", abstract: "Reinforcement learning algorithms often rely on estimators, like Monte Carlo tree search or Bellman lookaheads, to provide improved policies, values, and Q-values in training and inference. However, the current literature in multi-armed bandits related to sampling from a target distribution evaluates sampling efficiency rather than the quality of the resulting estimate. In this talk, I'll present the sampling bandit problem with a focus on estimate (target) generation as an explicit recommendation process. I will introduce recommender regret, a novel notion of regret that quantifies the accuracy of policy, value, and Q-value target recommendations in general regularised RL settings. Analysis reveals that simple algorithms like uniform sampling can achieve tight upper bounds on the recommender regret. Finally, we unify bandit- and search-based target improvement under Recommender MCTS, showing empirically across synthetic trees, arcade games, and scientific discovery tasks that recommenders can yield faster target convergence and superior downstream performance." },
	{ date: "2026-08-25", speaker: "Binxu Wang", website: "https://kempnerinstitute.harvard.edu/people/our-people/binxu-wang/", title: "How Far Does a Linear Theory of Diffusion Models Go?  Sampling, Learning, Receptive Fields, and Generalization", abstract: "Diffusion models are highly nonlinear generative systems, yet surprisingly much of their behavior can be understood through a simple linear theory. Taking a physicist’s approach, we study diffusion models with a linear score function. A useful duality makes this setting analytically tractable: Gaussian data imply a linear optimal score, while a linear score model captures the Gaussian statistics of arbitrary data. This regime yields exact predictions across several aspects of diffusion models. Its closed-form sampling dynamics accurately describe the early phase of pretrained nonlinear models, enabling analytical teleportation. The same theory shows how the receptive-field (jacobian) structure of trained networks emerges from data covariance. Further, the linear theory and random matrix analysis explain the sample consistency across training splits — the Gaussian statistics are much more consistent than the training samples. Finally, deep linear networks provide an analytical theory of learning dynamics and spectral bias. These dynamics closely predict those of MLP score networks, but not CNNs, revealing an important boundary of the linear picture: linear theory can describe the learned optimal solution well, while architecture strongly shapes how the model gets there. Together, these results show both how surprisingly far the linear/Gaussian limit goes—and where it begins to break down."},
	{ date: "2026-08-18", speaker: "Matthew Niedoba", website: "https://scholar.google.com/citations?user=uSl2vYwAAAAJ&hl=en", title: "Analytical Models of Diffusion Generalization", abstract: "Image diffusion models exhibit a curious generalization phenomenon - they produce remarkably similar samples under deterministic sampling procedures, despite differences in model architecture or training procedures. We investigate this phenomenon and propose analytical models to approximate this convergent behavior. We then discuss a unified framework that consolidates several of these analytical instances, and explore ways to improve generation quality within it." },
	{ date: "2026-08-11", speaker: "Claudia Merger", website: "https://claudiamer.github.io/", title: " What Do Generative Models Actually Learn? From Distributional Simplicity Bias to Memorization", abstract: "Generative models, such as diffusion models and normalizing flows must learn both a representation of the data distribution and a mechanism for sampling from it. However, learning an arbitrary high-dimensional density is generally considered prohibitively difficult because of the curse of dimensionality: without additional assumptions, the number of training samples required for density estimation can grow exponentially with the dimension. In practice, diffusion models appear to overcome this problem remarkably well, producing samples that are both novel and realistic. Under other conditions, however, they collapse towards a “memorizing solution,” reproducing individual training examples. At the same time, we still lack a precise description of what models that generalize rather than memorize actually learn. In this talk, I will discuss three theoretical concepts that help unravel this question. First, maximum-entropy models provide a tractable hypothesis class for formalizing which properties of a data distribution a generative model has learned. Second, dynamic distributional simplicity bias describes the tendency of neural networks to exploit simpler statistics before progressively incorporating higher-order structure. Finally, the theory of local memorization characterizes how and where diffusion models transition towards a memorizing solution." },
]



export function getSplitSchedule(talks = schedule_all) {
	const today = new Date().toLocaleDateString('en-CA'); // "2026-09-04"
	const upcoming = talks.filter(t => t.date >= today).sort((a, b) => a.date.localeCompare(b.date));
	const past = talks.filter(t => t.date < today).sort((a, b) => b.date.localeCompare(a.date));

    return { past, upcoming };
}

export const { past, upcoming } = getSplitSchedule();

<Layout
	title="Foundation of Deep Learning Seminar"
	description="Webpage for the internal Foundation of Deep Learning seminar of the Inria LabHC Malice and Ockham teams."
	pubDate={new Date('August 27 2026')}
>

	<p>
		This is a webpage for the internal "Foundation of Deep Learning" seminar of the Inria [Malice](https://labhc-malice.github.io/) and [Ockham](https://team.inria.fr/ockham/) teams.
		The seminar is a fully remote 45 min presentation, not recorded, and takes place on Tuesdays, 4 PM Paris time.
	</p>




### Upcoming talks

<div>
	{upcoming.map(({ date, speaker, website, title, abstract }) => (
		<article>
			<strong >{date}</strong>

			<div>
				{website ? (
					<a href={website}>{speaker}</a>
				) : (
					<span>{speaker}</span>
				)}
				<span>: <em>{title}</em></span>
			</div>

			<details>
				<summary>Abstract</summary>
				<p>{abstract}</p>
			</details>
		</article>
	))}
</div>


### Past talks

<div>
	{past.map(({ date, speaker, website, title, abstract }) => (
		<article>
			<strong >{date}</strong>

			<div>
				{website ? (
					<a href={website}>{speaker}</a>
				) : (
					<span>{speaker}</span>
				)}
				<span>: <em>{title}</em></span>
			</div>

			<details>
				<summary>Abstract</summary>
				<p>{abstract}</p>
			</details>
		</article>
	))}
</div>

</Layout>
