---
title: 'Discrete Flow  (old)'
menu: 'Discrete Flow Matching (old) (DFM)'
description: 'On discrete flow matching models and their core principles, and parallel with continuous flow matching'
pubDate: '2025-10-08'
heroImage: '../../assets/blog-placeholder-3.jpg'
order: 611
tags: ['Diffusion', 'Flow Matching']
typst: ['del', 'code']
---

import T from '../../components/typst/TypstMath.astro'
import Cetz from '../../components/typst/TypstCetz.astro'
import { Image } from 'astro:assets'
import diffusion from '../../assets/diffusion.svg'
import InlineSvg from '../../components/InlineSvg.astro'
import Counter from '../../components/numbering/Counter.astro'
import Ref from '../../components/numbering/Ref.astro'
import SudokuFlowMatching from '../../components/sudoku_flow_matching.jsx';
import DFMSampling from '../../components/DFMSampling.jsx';
import DFMLiveFigure from '../../components/DFMLiveFigure.astro';

<T defines='
#let anno(t) = $text(#luma(100), size: #10pt, (#t))$;
#let c = blue;
#let add(txt) = $text(#c, txt)$
#let KL(a,b) = $"KL"(#(a), #(b))$;
'/>

<style>{`
  .cite-tooltip {
    position: fixed;
    display: none;
    z-index: 2000;
    width: max-content;
    max-width: min(34rem, 92vw);
    background: #ffffff;
    color: #111827;
    border: 1px solid #d1d5db;
    border-radius: 8px;
    box-shadow: 0 10px 26px rgba(0, 0, 0, 0.15);
    padding: 0.55rem 0.7rem;
    font-size: 0.86rem;
    line-height: 1.45;
    white-space: normal;
  }

  .ref-entry {
    margin-bottom: 0.8rem;
  }

  .ref-main strong {
    font-weight: 700;
  }
`}</style>

<script is:inline>{`
  (() => {
    let tooltip;

    const ensureTooltip = () => {
      if (tooltip) return tooltip;
      tooltip = document.createElement('div');
      tooltip.className = 'cite-tooltip';
      document.body.appendChild(tooltip);
      return tooltip;
    };

    const getRefHtml = (link) => {
      const href = link.getAttribute('href') || '';
      if (!href.startsWith('#')) return '';
      const refAnchor = document.getElementById(href.slice(1));
      if (!refAnchor) return '';
      const refEntry = refAnchor.closest('.ref-entry');
      const refMain = refEntry ? refEntry.querySelector('.ref-main') : null;
      return refMain ? refMain.innerHTML : '';
    };

    const showTooltip = (link) => {
      const html = getRefHtml(link);
      if (!html) return;
      const tip = ensureTooltip();
      tip.innerHTML = html;
      tip.style.display = 'block';

      const rect = link.getBoundingClientRect();
      const margin = 10;
      let left = rect.left;
      let top = rect.bottom + 8;

      const tipRect = tip.getBoundingClientRect();
      if (left + tipRect.width > window.innerWidth - margin) {
        left = window.innerWidth - tipRect.width - margin;
      }
      if (left < margin) left = margin;
      if (top + tipRect.height > window.innerHeight - margin) {
        top = rect.top - tipRect.height - 8;
      }
      if (top < margin) top = margin;

      tip.style.left = left + 'px';
      tip.style.top = top + 'px';
    };

    const hideTooltip = () => {
      if (!tooltip) return;
      tooltip.style.display = 'none';
    };

    const bindCitations = () => {
      document.querySelectorAll('a.cite-ref').forEach((link) => {
        if (link.dataset.citeBound === '1') return;
        link.dataset.citeBound = '1';
        link.addEventListener('mouseenter', () => showTooltip(link));
        link.addEventListener('focus', () => showTooltip(link));
        link.addEventListener('mouseleave', hideTooltip);
        link.addEventListener('blur', hideTooltip);
      });
    };

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', bindCitations);
    } else {
      bindCitations();
    }
  })();
`}</script>

<div class="glossary">
<div class="glossary-content">
<label><input type="checkbox" id="cb-glossary" checked /> Glossary (keep visible)</label>

- **$\mathcal{V}$** vocabulary set; $|\mathcal{V}|$ is the vocabulary size
- **$\mathcal{S} = \mathcal{V}^S$** space of all possible sequences of length $S$ over the vocabulary
- **$\mathbf{x} = x^1 \dots x^S$** sequence of $S$ tokens, where each token $x^i$ takes values in $\mathcal{V}$
- **$\mathbf{e}_v$** one-hot vector in $\mathbb{R}^{|\mathcal{V}|}$ for token $v \in \mathcal{V}$
- **$m$** mask token $m \notin \mathcal{V}$, represented as the one-hot vector $(0, \dots, 0, 1) \in \mathbb{R}^{|\mathcal{V}|+1}$
- **$\mathbf{x}^{\neq i}$** sub-sequence of $\mathbf{x}$ at all positions except $i$;
- **$p_0$** source distribution (mask or uniform)
- **$p_{\text{mask}}$** mask source distribution: $p_{\text{mask}}(v) = \mathbb{I}(v = m)$
- **$p_{\text{unif}}$** uniform source distribution: $p_{\text{unif}}(v) = \frac{1}{|\mathcal{V}|}$
- **$p_1$** target distribution
- **$p_t$** marginal distribution at time $t \in [0,1]$; the probability path representing the trajectory that transports $p_0$ to $p_1$
- **$p_t(\mathbf{x} | \mathbf{z}) = \prod_{j=1}^S q_t(x^j | z^j)$** factorized conditional probability path given a conditioning variable $z$
- **$q_t(x | z)$** token-wise conditional distribution given $z$.
- **$q_t(x | x_0, x_1)$** source-target token-wise conditional distribution: $q_t(x | x_0, x_1) = \kappa_t \mathbb{I}(x = x_1) + (1 - \kappa_t) \mathbb{I}(x = x_0)$
- **$\kappa_t$** interpolation schedule; boundary conditions are $\kappa_0 = 0$ and $\kappa_1 = 1$, with a natural choice being $\kappa_t = t$
- **$u_t(\mathbf{x})_{\mathbf{y}}$** probability velocity: the instantaneous rate at which the sequence transitions from $\mathbf{x}$ to $\mathbf{y}$ at time $t$
- **$u_t^i(\mathbf{x})_v$** token-wise velocity at position $i$: the instantaneous rate at which the $i$-th token of $\mathbf{x}$ transitions to $v$ at time $t$
- **$u_t(\mathbf{x} | \mathbf{z})_{\mathbf{y}}$** conditional velocity field given $z$; it relates to the marginal velocity via expectation, formulated as $u_t(\mathbf{x})_{\mathbf{y}} = \mathbb{E}_{\substack{\mathbf{z}}}[u_t(\mathbf{x}\mid\mathbf{z})_{\mathbf{y}} \mid X_t = \mathbf{x}] $
- **$u_t(x | x_0, x_1)$** source-target token-wise conditional velocity: $u_t(x | x_0, x_1)_v = \frac{\dot{\kappa}_t}{1 - \kappa_t} \left( \mathbb{I}(v = x_1) - \mathbb{I}(v = x) \right)$
- **$u_{\theta,t}^i(\mathbf{x})_v$** learned velocity field parameterized by $\theta$
- **$P_{\theta,t}(X_1^i | \mathbf{x}_t)$** learned denoiser: at coordinate j, conditional probability that the $i$-th token at time 1 is $\mathbf{e}_j$, given current sequence $\mathbf{x}_t$

<label for="cb-glossary">Hide Glossary</label>
</div>
</div>

The goal of this blog post is to introduce discrete flow matching, explain its core principles, and provide a clear parallel with continuous flow matching. Discrete diffusion models were first introduced independently by two simultaneous papers at NeurIPS 2021 [<a class="cite-ref" href="#ref-1">1</a>,<a class="cite-ref" href="#ref-2">2</a>], later extended to continuous time [<a class="cite-ref" href="#ref-3">3</a>], made competitive with autoregressive models by SEDD [<a class="cite-ref" href="#ref-4">4</a>], and then extended with flow-based formulations in [<a class="cite-ref" href="#ref-5">5</a>] and [<a class="cite-ref" href="#ref-6">6</a>], mirroring the shift from diffusion to flow matching seen in continuous spaces.

### Discrete Flow Matching

#### Intuition of Discrete Flow Matching

Discrete flow matching was initially introduced as an alternative to autoregressive (AR) models for sequence generation. A fundamental limitation of current AR models is their reliance on the next-token prediction paradigm. This inherently restricts them to generating sequences one token at a time in a strict, left-to-right order.

While AR models excel at purely sequential generative task, such as answering a conversational prompt or writing a block of code from scratch, this architecture feels unnatural and inefficient for tasks that require bidirectional or global context:
- **Code infilling:** When missing tokens are scattered throughout a file, the model must condition heavily on both the few preceding and succeeding lines of code to fill the gaps accurately. To achieve this task with an AR model, we should give the full noisy code to the model before it starts filling, which is a counter-intuitive approach.
- **Solving Sudoku:** This is an inherently 2D spatial constraint problem. A number in a cell is constrained by its entire row, column, and subgrid, not just by the cells evaluated "before" it in a 1D sequence.
- **DNA sequence design:** Biological sequences rely on complex secondary structures and long-range dependencies. Parts of the sequence that are far apart often need to connect together like perfectly matched puzzle pieces so the molecule can fold into the right shape, making a purely left-to-right generation prone to structural errors.

By contrast, the paradigm of diffusion and flow matching is bidirectional. Throughout the generation process, every token is updated while simultaneously "seeing" both the past and the future of the sequence. This global context makes solving infilling, constraint-satisfaction games, and complex spatial tasks much more natural.

The primary objective of discrete flow matching is to define a process that smoothly transports a simple source distribution, denoted as $p_0$, into a complex target distribution (the actual data we want to generate), denoted as $p_1$. The intermediate distributions $p_t$ are then obtained by running this stochastic transport process over time $t \in [0,1]$.

In practice, we assume we have a dataset of $N$ samples $\mathcal{A} = \{a_1, \dots, a_N\}$ that follow the target distribution $p_1$.

The source distribution $p_0$ acts as our starting point for generation and is typically chosen to be either:
- **Mask distribution ($p_{\text{mask}}$):** All sequences start entirely filled with the mask token $m$.
- **Uniform distribution ($p_{\text{unif}}$):** All tokens in the starting sequence are sampled uniformly across the vocabulary $\mathcal{V}$.

For instance, if our goal is generating Sudoku, $p_0$ represents the theoretical distribution of initial states, like partially filled grids with masked unknowns ($p_{\text{mask}}$), while $\hat{p}_0$ denotes the $N$ empirical puzzle samples we actually draw. Conversely, the true target distribution $p_1$ defines the ideal space of all valid, completed Sudoku boards. Its empirical counterpart, $\hat{p}_1$, is formed by our training dataset $\mathcal{A}$, which consists of the exact $N$ solved grids corresponding to the initial puzzles in $\hat{p}_0$.

{/* Maybe add a dummy simulation of a sudoku that fills with x0, xt and x1, to draw a sudoku from pt = fill a sudoku from 0 to t?
The sudoku has the advantage to be constrained spatially so S is fixed and it's easy to make consistent simulations */}

<SudokuFlowMatching client:load />


#### Generation of discrete data

To generate discrete data, we rely on a time-dependent vector field $u$ that guides the sampling process.

**Definition (Probability Velocity)**:
Let $u : [0,1] \times \mathcal{S} \to \mathbb{R}^{|\mathcal{S}|}$ be a time-dependent vector field. We say that $u$ *generates* the probability path $p_t$ if, for a random variable $X_t$ constructed via the standard sampling algorithm, it holds that $X_t \sim p_t$.

Intuitively, $u_t(\mathbf{x})_{\mathbf{y}}$ is the instantaneous rate at which the sequence transitions from $\mathbf{x}$ to $\mathbf{y}$ at time $t$. Therefore, for any $\mathbf{x}, \mathbf{y} \in \mathcal{S}$:
$
  bb(P)(X_(t+h) = bold(y) | X_t = bold(x))
    = bb(I)(bold(x) = bold(y)) + h u_t(bold(x))_(bold(y)) + o(h)
$ <eq-prob-velocity>

If we index the state space as $\mathcal{S} = \{\mathbf{y}_1, \dots, \mathbf{y}_{|\mathcal{S}|}\}$ and choose $k$ such that $\mathbf{y}_k = \mathbf{x}$ (the current state), then the full transition-probability vector reads
$
  bb(P)(X_(t+h) = dot | X_t = bold(x)) =
  mat(
    h u_t(bold(x))_(bold(y)_1);
    dots.v;
    1 + h u_t(bold(x))_(bold(y)_k) quad text("where ") bold(y) = bold(x);
    dots.v;
    h u_t(bold(x))_(bold(y)_(|cal(S)|));
  )
  + o(h),
$ <eq-transition-vector>
where all off-diagonal entries are $h u_t(\mathbf{x})_{\mathbf{y}_j}$ and the diagonal entry at $\mathbf{y}=\mathbf{x}$ is $1 + h u_t(\mathbf{x})_{\mathbf{x}}$.

In practice, this means that at each sampling step, the next state is drawn according to the following categorical distribution update:
$
  bold(x)_(tau + h) ~ op("Cat")(bold(e)_(bold(x)_tau) + h u_tau(bold(x)_tau))
$ <eq-categorical-update>

This is the discrete counterpart of the continuous FM viewpoint. In continuous spaces, one defines a flow by the ODE
$
  cases(
    x(0) = x_0,
    partial_t x(t) = u(x(t), t), quad forall t in [0, 1],
  )
$ <eq-continuous-ode>
and obtains the time-$t$ law by pushing forward $p_0$.

Here, if we denote by $f_t^u : \mathcal{S} \times [0,1] \to \mathcal{S}$ the (random) map induced by simulating the CTMC with velocity field $u$ up to time $t$ from an initial state, then
$
  p_t = (f_t^u)_(text("#")) p_0,
$ <eq-pushforward>
**Important note**: unlike continuous FM, where sampling is deterministic and the flow map is the unique solution of the ODE, sampling here is inherently stochastic, so the flow map is random.

**Proposition (Mass Conservation for Velocity Fields).**
For the velocity field to preserve probability mass, it must satisfy:
1. **Positivity:** $u_t(\mathbf{x})_{\mathbf{y}} \ge 0$ for all $\mathbf{y} \neq \mathbf{x}$.
2. **Normalization:** $\sum_{\mathbf{y} \in \mathcal{S}} u_t(\mathbf{x})_{\mathbf{y}} = 0$ for all $\mathbf{x} \in \mathcal{S}$ and $t \in [0,1]$.

<details>
<summary>Justification</summary>
<div>
Hence, for each state $\mathbf{x}$,
$
  u_t(bold(x))_(bold(x)) = -sum_(bold(y) != bold(x)) u_t(bold(x))_(bold(y)).
$ <eq-diagonal-rate>
Positivity guarantees nonnegative off-diagonal transition probabilities:
$
  bb(P)(X_(t+h) = bold(y) | X_t = bold(x))
    = h u_t(bold(x))_(bold(y)) + o(h) >= 0,
  quad bold(y) != bold(x).
$ <eq-positive-off-diagonal>
Normalization guarantees total mass conservation:
$
  sum_(bold(y) in cal(S)) bb(P)(X_(t+h) = bold(y) | X_t = bold(x))
    = 1 + h sum_(bold(y) in cal(S)) u_t(bold(x))_(bold(y)) + o(h)
    = 1 + o(h).
$ <eq-total-mass>
</div>
</details>

The relationship between this velocity field and the marginal probability path is formalized by the Kolmogorov Equation.

**Theorem (Kolmogorov Equation)**:
The vector field $u$ generates $p_t$ if and only if for all $\mathbf{x}, \mathbf{y} \in \mathcal{S}$:
$
  d/(d t) p_t(bold(y))
    = sum_(bold(x) in cal(S)) u_t(bold(x))_(bold(y)) p_t(bold(x))
$ <eq-kolmogorov>

This directly parallels continuous flow matching: there, the deterministic ODE induces the continuity equation
$
  partial_t p_t + nabla dot.c (u_t p_t) = 0.
$ <eq-continuity>
Together, @eq-prob-velocity and @eq-kolmogorov separate the local jump rates from the global evolution of the probability path.

In discrete spaces, the Kolmogorov equation plays the same role and governs how probability mass moves under the velocity field.

<details>
<summary>Proof</summary>
<div>
We can derive this directly by expanding the marginal probability at $t+h$ using the law of total probability and our velocity definition:

$
  p_(t+h)(bold(y)) &= bb(P)(X_(t+h) = bold(y)) \
    &= sum_(bold(x) in cal(S)) bb(P)(X_(t+h) = bold(y) | X_t = bold(x)) bb(P)(X_t = bold(x)) \
    &= sum_(bold(x) in cal(S)) (bb(I)(bold(y) = bold(x)) + h u_t(bold(x))_(bold(y)) + o(h)) p_t(bold(x)) \
    &= p_t(bold(y)) + h sum_(bold(x) in cal(S)) u_t(bold(x))_(bold(y)) p_t(bold(x)) + o(h)
$ <eq-kolmogorov-proof-expansion>

Rearranging the terms and taking the limit as $h \to 0$ gives:

$
  lim_(h -> 0) (p_(t+h)(bold(y)) - p_t(bold(y))) / h
    = sum_(bold(x) in cal(S)) u_t(bold(x))_(bold(y)) p_t(bold(x))
$ <eq-kolmogorov-proof-limit>

For the reverse implication (if the equation holds, then $u$ generates $p_t$), let $\tilde{p}_t$ denote the true marginal probability path generated by simulating the continuous-time Markov chain with the velocity field $u_t$, starting from the initial distribution $\tilde{p}_0 = p_0$. By the forward implication proven above, $\tilde{p}_t$ strictly satisfies the Kolmogorov forward equation:

$
  d/(d t) tilde(p)_t(bold(y))
    &= sum_(bold(x) in cal(S)) u_t(bold(x))_(bold(y)) tilde(p)_t(bold(x))
$ <eq-kolmogorov-proof-reverse>

</div>
</details>


### References

<div style={{ fontSize: '0.92rem', lineHeight: '1.55' }}>

<div class="ref-entry">
  <span id="ref-1"></span>
  <div class="ref-main">
    1. <strong>Argmax Flows and Multinomial Diffusion: Learning Categorical Distributions</strong> <strong><a href="https://arxiv.org/pdf/2102.05379">[PDF]</a></strong><br />
    Emiel Hoogeboom, Didrik Nielsen, Priyank Jaini, Patrick Forre, Max Welling. NeurIPS 2021.
  </div>
</div>

<div class="ref-entry">
  <span id="ref-2"></span>
  <div class="ref-main">
    2. <strong>Structured Denoising Diffusion Models in Discrete State-Spaces</strong> <strong><a href="https://arxiv.org/pdf/2107.03006">[PDF]</a></strong><br />
    Jacob Austin, Daniel D. Johnson, Jonathan Ho, Daniel Tarlow, Rianne van den Berg. NeurIPS 2021.
  </div>
</div>

<div class="ref-entry">
  <span id="ref-3"></span>
  <div class="ref-main">
    3. <strong>A Continuous Time Framework for Discrete Denoising Models</strong> <strong><a href="https://arxiv.org/pdf/2205.14987">[PDF]</a></strong><br />
    Andrew Campbell, Joe Benton, Tom Rainforth, George Deligiannidis, Arnaud Doucet. NeurIPS 2022.
  </div>
</div>

<div class="ref-entry">
  <span id="ref-4"></span>
  <div class="ref-main">
    4. <strong>Discrete Diffusion Modeling by Estimating the Ratios of the Data Distribution</strong> <strong><a href="https://arxiv.org/pdf/2310.16834">[PDF]</a></strong><br />
    Aaron Lou, Chenlin Meng, Stefano Ermon. ICML 2024.
  </div>
</div>

<div class="ref-entry">
  <span id="ref-5"></span>
  <div class="ref-main">
    5. <strong>Generative Flows on Discrete State-Spaces: Enabling Multimodal Flows with Applications to Protein Co-Design</strong> <strong><a href="https://arxiv.org/pdf/2402.04997">[PDF]</a></strong><br />
    Andrew Campbell, Jason Yim, Regina Barzilay, Tom Rainforth, Tommi Jaakkola. ICML 2024.
  </div>
</div>

<div class="ref-entry">
  <span id="ref-6"></span>
  <div class="ref-main">
    6. <strong>Discrete Flow Matching</strong> <strong><a href="https://arxiv.org/pdf/2407.15595">[PDF]</a></strong><br />
    Itai Gat, Tal Remez, Neta Shaul, Felix Kreuk, Ricky T. Q. Chen, Gabriel Synnaeve, Yossi Adi, Yaron Lipman. NeurIPS 2024.
  </div>
</div>

</div>
